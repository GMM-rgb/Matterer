export class Matterer {
    ValidateInputType({ VALUE, TYPE_DEFINITION }) {
        let TypeInputMatches = false;
        if (VALUE !== null && TYPE_DEFINITION !== null) {
        }
        return TypeInputMatches;
    }
}
